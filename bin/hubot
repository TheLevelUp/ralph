#!/bin/sh

set -e

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

# Source .env if it's present
[ -f .env ] && . $PWD/.env

exec node_modules/.bin/hubot --name ralph "$@"
